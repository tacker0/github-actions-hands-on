name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: tacker0/github-actions-hands-on@main
      - uses: actions/checkout@v7
      - run: cat README.md
